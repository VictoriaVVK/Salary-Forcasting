# Salary-Forcasting
Salary prediction using machine learning regression models. The project uses a public UCI Adult dataset loaded via OpenML and compares Linear Regression, Decision Tree, and Random Forest for predicting annual salary.
